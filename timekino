p = peripheral.wrap("top")

term.redirect(p)
        while true do
            time = os.time()
            if time == 10 then
                shell.run("kinofilm")
            else
            end
        end
term.restore()